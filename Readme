# R-Topology Planner (Simulation Code)

This repository provides the simulation code and plotting scripts for the paper:

> *"R-Topology for Motion Planning under Directional and Irreversible Constraints"*

## Contents
- `rrt_sim.py` : Python implementation of Standard RRT* and R-Constrained RRT* with plotting.
- `results_2d.csv` : Sample results (30 runs per scenario).
- `results_uav.csv` : UAV scenario results.
- `figures/` : Example plots generated by the scripts.

## Requirements
- Python >= 3.9
- NumPy, Matplotlib, SciPy, PIL

## Usage
```bash
python rrt_sim.py
